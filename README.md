# CS-362-Project

UIC CS 362 — “Who is the Fastest?” Arduino race setup (master + two lane slaves, I2C). Firmware is under `firmware/`. Open **`firmware/mainTest`** for the master sketch (folder name must match `mainTest.ino`). Lane 1 slave: **`firmware/slave_lane_1`**. Older master sketches were moved to `firmware/archive_master_extras/`.
