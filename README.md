# CS-362-Project

UIC CS 362 — “Who is the Fastest?” Arduino race setup (master + two lane slaves, I2C). Firmware is under `firmware/`. **Master (lane 1 only):** open **`firmware/master`** (`master.ino`). **Lane 1 slave:** **`firmware/slave_lane_1`**. Old / extra sketches: **`firmware/archive_master_extras/`**.
